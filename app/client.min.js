var W=process.env.SITE_NAME||window.location.pathname,b=process.env.PORT||8080,a=new WebSocket(`wss://${W}:${b}`);a.addEventListener("open",()=>{a.send("[\u231A\uFE0F Tokei] Connection from client established.")});a.addEventListener("message",async(r)=>{if(r.data!=="change")return;window.location.reload()});
